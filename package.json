{
  "name": "fbi-blog",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:ssg",
    "build:spa": "npm run prebuild && valaxy build",
    "build:ssg": "npm run prebuild && valaxy build --ssg",
    "dev": "npm run prebuild && valaxy",
    "prebuild": "node scripts/add-date.cjs && node scripts/extract-cover.cjs",
    "fuse": "valaxy fuse",
    "rss": "valaxy rss",
    "serve": "vite preview"
  },
  "dependencies": {
    "valaxy": "1.0.0-rc.9",
    "valaxy-theme-sakura": "^0.10.3-alpha.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vue": "^3.5.42"
  }
}
